%h1 Users
%ul
  - @users.each do |u|
    %li 
      %strong= u.username
      \ #{u.email}
      = link_to "Edit name", edit_user_path(u)
      / = link_to "Delete user", c, method: :delete, data: {confirm: "Are you sure?"}

= link_to "Make a new user", new_user_path